# Database settings (used by docker-compose and application)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ramon
DB_USER=ramon
DB_PASSWORD='' # Use a strong password (generate: openssl rand -base64 24)

# API Keys
OPENAI_API_KEY='' # API key for the OpenAI chat model
